/* 1. Cria um código que recebe um número inteiro positivo e em seguida utiliza um while loop para contar de 1 até um número fornecido.

2. A função deve imprimir cada número no console à medida que conta.
*/

let contador = 1;
let numeros = 10;

while (contador <= numeros) {
  console.log(contador);
  contador++;
}
